version: 2.1
orbs:
  win: circleci/windows@2.4.1
jobs:
  lin-build:
    machine:
      image: ubuntu-2004:current
    resource_class: medium
    steps:
      - checkout:
          path: ~/project
      - run: sudo apt install python3-dev
      - run: env PYTHON_CONFIGURE_OPTS="--enable-shared" pyenv install -v 3.8.10
      - run: mkdir -p ~/project/artifacts
      - run: curl -sSL https://install.python-poetry.org | python3 -
      - run: ~/.local/bin/poetry install
      - run: ~/.local/bin/poetry run task build
      - run: ls -al
      - run: mv build/* artifacts/
      - persist_to_workspace:
          # Must be an absolute path, or relative path from working_directory
          root: ~/project/artifacts
          # Must be relative path from root
          paths:
            - echo-output
  win-build:
    executor:
      name: win/default
      shell: powershell.exe
    steps:
      - checkout
      - attach_workspace:
          at: ./artifacts
      - run: Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
      - run: choco install python --version=3.8.10
      - run: (Invoke-WebRequest -Uri https://install.python-poetry.org -UseBasicParsing).Content | python -
      - run:
            name: "Poetry install" 
            command: "%APPDATA%\\Python\\Scripts\\poetry install"
            shell: cmd.exe
      - run:
            name: "Poetry build" 
            command: "%APPDATA%\\Python\\Scripts\\poetry run task build"
            shell: cmd.exe
      - run: dir
      - run:
          name: "Copy executable to artifacts"
          command: mv build/* artifacts/
          shell: bash.exe
      - run: echo "Trying out workspaces" > artifacts/echo-output2
      - persist_to_workspace:
          # Must be an absolute path, or relative path from working_directory
          root: artifacts
          # Must be relative path from root
          paths:
            - "*.exe"
  pgr:
    docker:
      - image: cibuilds/github:0.10
    steps:
      - attach_workspace:
          at: ./artifacts
      - run:
          name: "Publish Release on GitHub"
          command: |
            VERSION=$(my-binary --version)
            ghr -t ${GITHUB_TOKEN} -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -c ${CIRCLE_SHA1} -delete ${VERSION} ./artifacts/
      
workflows:
  version: 2
  windows:
    jobs:
      - win-build
  linux:
    jobs:
      - lin-build